// Import the Flutter Material package, which contains UI components for building Flutter apps.
import 'package:flutter/material.dart';

// Define constant color variables for easy reference throughout the code.
const black = Colors.black; // Black color constant
const white = Colors.white; // White color constant
const red = Colors.red; // Red color constant
const grey = Colors.grey; // Grey color constant

// Create a variable 'golden' and assign it a color shade derived from the yellow color constant.
Color golden = Colors.yellow.shade800;  // Golden color with shade 800
